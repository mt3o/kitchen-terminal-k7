name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "24"
          cache: npm

      - run: npm ci

      - name: ESLint
        run: npm run lint

      # Enforces the theming rule (components read only CSS custom properties,
      # never hardcoded colours/fonts/radii) and the Safari 15.0 CSS floor —
      # the target device is an iPad Air 2 capped at iPadOS 15.
      - name: Design-token contract
        run: npm run lint:tokens

      - name: Theme drift

        # design-system/tokens.css is generated from the theme file. A committed copy

        # that has drifted means Storybook proves a design the kiosk does not have.

        run: npm run tokens:check


      - name: Tests
        run: npm test

      - name: Typecheck + build
        run: npm run build

      - name: Storybook build
        # Catches a story that no longer compiles, and the component-registration
        # failure that a green app build can hide.
        run: npm run build:storybook
