{
  "mcpServers": {
    "agentic-memory": {
      "type": "stdio",
      "command": "sh",
      "args": [
        "-c",
        "cd \"$CLAUDE_PROJECT_DIR\" && PATH=\"$HOME/.local/bin:$PATH\" exec agentic-memory-mcp"
      ],
      "env": {
        "MEMORY_DB_PATH": "context/memory-graph.db"
      }
    }
  }
}
