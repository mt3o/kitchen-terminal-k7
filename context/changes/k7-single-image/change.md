# k7-single-image

status: open
created: 2026-09-10

## Goal
Add a new "image" card type — a static image from a configured URL, no
rotation/polling — plus a slightly shorter footer bar; wire it into
layout.yaml alongside the existing carousel/grid so an "image carousel"
is just the generic carousel holding image-card slides.

memory_goal: c7fd59cf-268c-473e-acb1-559187232e85
